# game-content-screenshots

/game-content-screenshots
│── /tests
│   │── featurePopupTest.js          # Test script
│   │── mainGameTest.js   # Another test file (optional)
│
│── /utils
│   │            # Game and language configuration
│   │── networkUtils.js      # Handles network requests and XML parsing
│   │── screenshotUtils.js   # Handles screenshots and scrolling
|   │── gameUtils.js        # 
|   │── keyboardUtils.js    # 
│
|── index.js #
|── config.js
│── package.json
|── package-lock.json
│── node_modules
│── README.md


npm install

run all tests:

node index.js

run one test:

node index.js featurePopupTest  //'featurePopupTest' is name of the test
